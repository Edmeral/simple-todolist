Simple (too simple?) todolist app written in Node.JS using Express and the template engine EJS.

To test it locally you should have Node.JS installed, then clone the repo and run:

```
npm install
node app.js
```
